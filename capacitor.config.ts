import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tbmulhern.sbmsubfinder',
  appName: 'SBM SubFinder',
  webDir: 'build'
};

export default config;
